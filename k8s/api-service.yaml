apiVersion: v1
kind: Service
metadata:
  name: licitaciones-api
  namespace: licitaciones
spec:
  selector: {app: licitaciones-api}
  type: ClusterIP
  ports: [{name: http, port: 8080, targetPort: 8080}]
